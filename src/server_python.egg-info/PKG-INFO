Metadata-Version: 2.4
Name: server-python
Version: 0.1.0
Summary: MATLAB server app migration to a Python CLI
Author: University Project
Requires-Python: >=3.11
Description-Content-Type: text/markdown

# Server_Python

This repository now contains three CLI projects:

- Server CLI for TCP server operations
- Transform CLI for conversion and transform scripts
- Analysis CLI for analysis workflows

## 1) Server CLI

Python CLI migration of the MATLAB server app. The CLI keeps the same core behavior:

- Multiple TCP listeners (default ports: 5001, 5002, 5003, 5004)
- Client tracking keyed by IP (MATLAB parity)
- Incoming data parsed as uint32 words
- Termination marker handling for `0xFFFFFFFF`
- Binary append writes to per-client files

## Requirements

- Python 3.11+

## Install

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e .
```

## Run

```powershell
server-cli --ports 5001 5002 5003 5004 --output-dir .
```

## CLI Commands

- `start [--ports 5001 5002 ...]`
- `stop`
- `list`
- `send --client <ip> [--time <seconds>]`
- `transfer --client <ip>`
- `status`
- `quit`

Notes:

- The `send` command always transmits the literal string `data` to mirror MATLAB.
- `--time` is accepted for compatibility but not used in payload construction.
- By default, incoming payload words are written immediately.
- Use `--manual-transfer-only` to buffer incoming data and flush with `transfer`.

## Output Files

Received uint32 payload data is appended to:

- `ClientData_<client-ip>.bin`

IPv6 separators are sanitized (`:` -> `_`) in file names.

## Tests

```powershell
python -m unittest discover -s tests -v
```

## 2) Transform CLI

The Transform CLI wraps existing transform scripts in the repository root.

Run examples:

```powershell
transform-cli bin-to-hex input.bin output.txt
transform-cli pipeline
transform-cli pipeline-expt2
transform-cli run-script pipeline.py
```

## 3) Analysis CLI

The Analysis CLI wraps existing analysis scripts in the repository root.

List available workflows:

```powershell
analysis-cli list
```

Run a workflow:

```powershell
analysis-cli run frequency
analysis-cli run sync <file1> <file2> <output_dir>
analysis-cli run-script frequencyAnalysis.py
```

Note:

- Many existing analysis scripts have hardcoded paths/parameters.
- The CLI integration keeps current behavior and exposes a clean command surface.
